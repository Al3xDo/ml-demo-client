import React from 'react';
import loading from "./asset/loading.gif"
const Loading = () => {
    return (
        <div style={{ marginTop: "12rem" }}>
            <img src={loading} alt="loading-spint" />

        </div>
    );
}

export default Loading;
